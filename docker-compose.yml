services:
  agent-mail:
    image: agent-mail:latest
    command: --transport sse --addr :9090
    ports:
      - "9090:9090"
    volumes:
      - ~/.agent-mail:/root/.agent-mail:ro
    restart: unless-stopped
