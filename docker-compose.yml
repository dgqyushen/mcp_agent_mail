services:
  agent-mail:
    image: agent-mail:latest
    command: --transport sse --addr :8080
    ports:
      - "8080:8080"
    volumes:
      - ~/.agent-mail:/root/.agent-mail:ro
    restart: unless-stopped
