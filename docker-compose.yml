services:
  agent-mail:
    image: dgqyushen/agent-mail:latest
    command: ["--addr", ":8080", "--db-path", "/data/agent-mail.db"]
    environment:
      - HOME=/root
    ports:
      - "8080:8080"
    volumes:
      - agent-mail-data:/data

volumes:
  agent-mail-data:
